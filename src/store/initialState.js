import chat from '../modules/chat/initialState';
import inputs from '../modules/inputs/initialState';
import core from '../modules/core/initialState';

const initialState = {
  chat,
  inputs,
  core
}

export default initialState;